# タスク管理
## 完了済み
- T01: ドキュメント初期セット作成（要件・設計・実装計画・ロードマップ）
- T02: 手書きパーサの雛形作成
- T03: `let` 文の構文解析 + AST 生成
- T04: `|>` の優先順位と結合規則の実装
- T05: Resolver と Parser の接続 + eval + `fn` + REPL 統合
- T06: `match` MVP（パターン: リテラル/ワイルドカード/変数/リスト）
- T07: `with` MVP（パターン束縛 + `else` フォールバック）
- T08: `io do ... end` 境界制御 + `run_text` 組み込み
- T09: `run_text` のエラー整形（CommandFailed / CommandIo 診断）
- T10: `set` 文（`let mut` で宣言した変数を `set` で更新、不変変数への `set` は静的拒否）
## 次スプリント候補
- `when` ガード（match アーム条件）
- Stream ベースの `map` / `where` / `each`
- JSON / Text 変換ブリッジ
## Definition of Done
- `cargo test` が成功
- 追加した仕様に対応するテストを同時追加
- 失敗時に span 付き診断が確認できる
